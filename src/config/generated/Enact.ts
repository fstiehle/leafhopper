import IEnact from '../../interfaces/IEnact'

const enact: IEnact = (tokenState: number[], taskID: number, participantID: number): number[] => {
  console.warn("No Enactment function generated!");
  return tokenState;
}

export default enact;