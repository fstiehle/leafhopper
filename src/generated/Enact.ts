import { IEnact } from '../interfaces/IEnforcement'

const enact: IEnact = (tokenState: number[], taskID: number): number[] => {
  console.warn("No Enactment function generated!");
  return tokenState;
}
export default enact;